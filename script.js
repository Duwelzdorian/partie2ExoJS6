function changeImage_2(element) {
  // concatener les attributs de l'element pour remplacer par un autre image
  element.src = 'images/' + element.id + '_2.jpg';
}
function changeImage(element) {
  element.src = 'images/' + element.id + '.jpg';
}
